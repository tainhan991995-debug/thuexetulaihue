import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowRight, Check, ChevronRight, MapPin, MessageCircle, Phone } from "lucide-react";
import { BookingCta } from "../../../components/booking-cta";
import { blogPostBySlug, blogPosts, getBlogSections } from "../../../data/blog-posts";
import { seoPageBySlug, seoPages } from "../../../data/seo-pages";
import { createArticleSchema, createBreadcrumbSchema, createFaqPageSchema, createWebPageSchema, siteUrl } from "../../../data/structured-data";

const hotline = "0986 789 715";
const hotlineHref = "tel:0986789715";
const zaloHref = "https://zalo.me/0986789715";

export function generateStaticParams() {
  return blogPosts.map(({ slug }) => ({ slug }));
}

export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> {
  const { slug } = await params;
  const post = blogPostBySlug[slug];
  if (!post) return {};
  const canonical = `${siteUrl}/blog/${post.slug}`;
  return {
    title: post.title,
    description: post.description,
    alternates: { canonical },
    openGraph: { type: "article", locale: "vi_VN", url: canonical, title: post.title, description: post.description, images: [{ url: "/og.png", width: 1200, height: 630, alt: post.imageAlt }] },
    twitter: { card: "summary_large_image", title: post.title, description: post.description, images: ["/og.png"] },
  };
}

function Header() {
  return <header className="border-b border-white/30 bg-white/90 shadow-[0_2px_20px_rgba(13,59,102,.06)] backdrop-blur-xl"><div className="mx-auto flex min-h-[82px] max-w-7xl items-center justify-between gap-5 px-5 py-3 lg:px-8"><Link href="/" className="group flex items-center gap-3.5"><span className="grid h-12 w-12 place-items-center overflow-hidden rounded-2xl bg-white shadow-float"><Image src="/images/logo.jpg" width={48} height={48} alt="Logo TRẠM NHÀ MINT" className="h-full w-full object-contain" /></span><span><span className="block font-heading text-lg font-extrabold leading-none tracking-[.015em] text-ink md:text-2xl">TRẠM NHÀ MINT</span><span className="mt-1 hidden text-sm font-medium text-[#0F9D8C] sm:block">Cho thuê xe tự lái - Huế</span></span></Link><nav aria-label="Điều hướng chính" className="hidden items-center gap-5 xl:flex"><Link href="/thue-xe-tu-lai-hue" className="text-sm font-semibold text-slate-600 hover:text-ink">Thuê xe tự lái</Link><Link href="/thue-xe-vinfast-hue" className="text-sm font-semibold text-slate-600 hover:text-ink">Xe VinFast</Link><Link href="/bang-gia" className="text-sm font-semibold text-slate-600 hover:text-ink">Bảng giá</Link><Link href="/blog" className="text-sm font-bold text-teal">Blog</Link><Link href="/lien-he" className="text-sm font-semibold text-slate-600 hover:text-ink">Liên hệ</Link></nav><a href={hotlineHref} className="inline-flex shrink-0 items-center gap-2 rounded-full bg-[#0D4C92] px-4 py-2.5 text-sm font-bold text-white shadow-sm transition hover:scale-[1.03] hover:shadow-lg"><Phone size={18} className="text-sand" /> {hotline}</a></div></header>;
}

function Breadcrumb({ label }: { label: string }) {
  return <nav aria-label="Breadcrumb" className="mx-auto max-w-7xl px-5 pt-7 text-sm lg:px-8"><ol className="flex flex-wrap items-center gap-2 text-slate-500"><li><Link href="/" className="hover:text-teal">Trang chủ</Link></li><li aria-hidden="true"><ChevronRight size={15} /></li><li><Link href="/blog" className="hover:text-teal">Blog</Link></li><li aria-hidden="true"><ChevronRight size={15} /></li><li className="font-semibold text-ink">{label}</li></ol></nav>;
}

export default async function BlogPostPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  const post = blogPostBySlug[slug];
  if (!post) notFound();

  const canonical = `${siteUrl}/blog/${post.slug}`;
  const sections = getBlogSections(post);
  const serviceLinks = Array.from(new Set([...post.serviceLinks, ...seoPages.map(({ slug: serviceSlug }) => serviceSlug)])).slice(0, 5).map((serviceSlug) => seoPageBySlug[serviceSlug]);
  const relatedPosts = Array.from(new Set([...post.relatedPosts, ...blogPosts.map(({ slug: blogSlug }) => blogSlug)])).filter((relatedSlug) => relatedSlug !== post.slug).slice(0, 5).map((relatedSlug) => blogPostBySlug[relatedSlug]);
  const midwaySectionIndex = Math.floor((sections.length - 1) / 2);
  const articleSchema = { "@context": "https://schema.org", "@graph": [createArticleSchema({ headline: post.h1, description: post.description, path: `/blog/${post.slug}`, image: post.image, imageAlt: post.imageAlt, datePublished: "2026-08-04T14:51:20+07:00" }), createWebPageSchema({ name: post.title, description: post.description, path: `/blog/${post.slug}`, image: post.image }), createBreadcrumbSchema([{ name: "Trang chủ", path: "/" }, { name: "Blog", path: "/blog" }, { name: post.title, path: `/blog/${post.slug}` }], `/blog/${post.slug}`), ...(post.video ? [{ "@type": "VideoObject", name: post.h1, description: post.description, thumbnailUrl: `${siteUrl}${post.video.poster}`, contentUrl: `${siteUrl}${post.video.src}`, uploadDate: post.video.uploadDate ?? "2026-08-27T00:00:00+07:00", publisher: { "@type": "Organization", name: "TRẠM NHÀ MINT", url: siteUrl } }] : [])] };
  const faqSchema = createFaqPageSchema(post.faqs, `/blog/${post.slug}`);

  return <><Header /><main><Breadcrumb label={post.title} /><section className="pb-14 pt-8 sm:pb-20"><div className="mx-auto grid max-w-7xl gap-10 px-5 lg:grid-cols-[1fr_.9fr] lg:items-center lg:px-8"><div><p className="flex items-center gap-2 text-xs font-extrabold uppercase tracking-[.18em] text-teal"><span className="h-px w-8 bg-sand" /> {post.category} · Huế</p><h1 className="mt-4 max-w-3xl font-heading text-4xl font-black leading-[1.1] tracking-[-.03em] text-ink sm:text-5xl">{post.h1}</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-slate-600">{post.lead}</p><div className="mt-7 flex flex-wrap gap-3"><a href={zaloHref} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-xl bg-teal px-5 py-3.5 text-sm font-bold text-white transition hover:bg-[#207a70]"><MessageCircle size={18} /> Nhắn Zalo xác nhận lịch</a><Link href="/bang-gia" className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-5 py-3.5 text-sm font-bold text-ink transition hover:border-teal hover:text-teal">Xem bảng giá <ArrowRight size={17} /></Link></div></div><div className={`overflow-hidden rounded-[2rem] border border-slate-100 bg-mist p-2 shadow-soft ${post.video ? "flex justify-center" : ""}`}>{post.video ? <video controls playsInline preload="none" poster={post.video.poster} aria-label={post.video.alt} className="aspect-[9/16] w-full max-w-[280px] rounded-[1.55rem] object-contain sm:max-w-[320px] lg:h-[440px] lg:w-auto lg:max-w-none"><source src={post.video.src} type="video/mp4" />Trình duyệt của bạn không hỗ trợ phát video.</video> : <Image src={post.image} width={960} height={640} sizes="(min-width: 1024px) 45vw, 100vw" alt={post.imageAlt} className="h-[330px] w-full rounded-[1.55rem] object-cover object-center sm:h-[410px]" />}</div></div></section><article className="bg-white py-16 sm:py-20"><div className="mx-auto grid max-w-7xl gap-12 px-5 lg:grid-cols-[minmax(0,1fr)_300px] lg:px-8"><div className="min-w-0">{post.highlights?.length ? <section className="mb-12 rounded-[1.5rem] bg-mist p-6 sm:p-8"><p className="text-xs font-extrabold uppercase tracking-[.18em] text-teal">Điểm đáng chú ý trong video</p><h2 className="mt-3 font-heading text-2xl font-bold text-ink sm:text-3xl">Vì sao VinFast VF3 phù hợp để vi vu Huế?</h2><div className="mt-6 grid gap-4">{post.highlights.map((highlight) => <div key={highlight.title} className="rounded-2xl bg-white p-5 shadow-sm"><h3 className="font-heading text-lg font-bold text-ink">{highlight.title}</h3><p className="mt-2 leading-7 text-slate-600">{highlight.description}</p></div>)}</div></section> : null}<div className="space-y-12">{sections.map((section, index) => <div key={section.heading}><section><h2 className="font-heading text-2xl font-bold leading-tight text-ink sm:text-3xl">{section.heading}</h2><div className="mt-5 space-y-5 text-[16px] leading-8 text-slate-600">{section.paragraphs.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}</div><h3 className="mt-6 font-heading text-xl font-bold text-ink">{section.h3}</h3><ul className="mt-4 grid gap-3 sm:grid-cols-2">{section.checklist.map((item) => <li key={item} className="flex gap-2.5 rounded-xl bg-mist px-4 py-3 text-sm leading-6 text-slate-700"><Check size={18} className="mt-0.5 shrink-0 text-teal" />{item}</li>)}</ul></section>{index === midwaySectionIndex && <div className="mt-12"><BookingCta placement="middle" /></div>}</div>)}</div><section className="mt-14" id="cau-hoi"><p className="text-xs font-extrabold uppercase tracking-[.18em] text-teal">Câu hỏi thường gặp</p><h2 className="mt-3 font-heading text-3xl font-bold text-ink">Giải đáp về {post.category.toLowerCase()}</h2><div className="mt-7 divide-y divide-slate-100 overflow-hidden rounded-[1.5rem] border border-slate-100 bg-white shadow-soft">{post.faqs.map((faq) => <details key={faq.question} className="group p-5"><summary className="cursor-pointer list-none font-heading font-bold text-ink">{faq.question}</summary><p className="mt-3 leading-7 text-slate-600">{faq.answer}</p></details>)}</div></section><div className="mt-14"><BookingCta placement="end" /></div></div><aside className="h-fit rounded-[1.5rem] bg-mist p-6 lg:sticky lg:top-6"><MapPin size={22} className="text-teal" /><h2 className="mt-4 font-heading text-xl font-bold text-ink">Khám phá thêm</h2><p className="mt-2 text-sm leading-6 text-slate-500">Xem thêm dịch vụ và các bài hướng dẫn hữu ích trước khi chọn xe.</p><div className="mt-5 grid gap-2">{serviceLinks.map((service) => <Link key={service.slug} href={`/${service.slug}`} className="flex items-center justify-between rounded-xl bg-white px-4 py-3 text-sm font-bold text-ink shadow-sm transition hover:bg-teal hover:text-white">{service.label}<ArrowRight size={16} /></Link>)}</div><div className="mt-7 border-t border-slate-200 pt-6"><p className="text-xs font-extrabold uppercase tracking-[.14em] text-teal">Bài viết liên quan</p><div className="mt-3 grid gap-2">{relatedPosts.map((related) => <Link key={related.slug} href={`/blog/${related.slug}`} className="rounded-xl bg-white px-4 py-3 text-sm font-bold leading-5 text-ink shadow-sm transition hover:text-teal">{related.title}</Link>)}</div></div><Link href="/lien-he" className="mt-6 inline-flex items-center gap-2 text-sm font-bold text-teal hover:text-ink">Liên hệ TRẠM NHÀ MINT <ArrowRight size={16} /></Link></aside></div></article></main><footer className="border-t border-slate-100 bg-white"><div className="mx-auto flex max-w-7xl flex-col gap-4 px-5 py-9 text-sm text-slate-500 sm:flex-row sm:items-center sm:justify-between lg:px-8"><Link href="/" className="flex items-center gap-3 font-heading font-extrabold text-ink"><span className="grid h-10 w-10 place-items-center overflow-hidden rounded-xl bg-white shadow-soft"><Image src="/images/logo.jpg" width={40} height={40} alt="Logo TRẠM NHÀ MINT" className="h-full w-full object-contain" /></span> TRẠM NHÀ MINT</Link><div className="flex flex-wrap gap-x-5 gap-y-2"><Link href="/thue-xe-tu-lai-hue">Thuê xe tự lái Huế</Link><Link href="/bang-gia">Bảng giá</Link><Link href="/blog">Blog</Link><Link href="/lien-he">Liên hệ</Link></div></div></footer><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(articleSchema) }} /><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqSchema) }} /></>;
}
